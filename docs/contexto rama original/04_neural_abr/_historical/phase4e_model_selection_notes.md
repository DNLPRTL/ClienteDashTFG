# Phase 4E model selection notes

## Selection Status

No model is selected for Phase 4F from this run.

The run supports only:

```text
PHASE4E_SYNTHETIC_SMOKE_PASS_READY_FOR_TRACE_DATA
```

It does not support:

```text
PHASE4E_OFFLINE_CANDIDATE_READY_FOR_PHASE4F
```

## Reasoning

The Phase 4E Tier 0 smoke verified the mechanics:

- dataset build passed;
- dataset validation passed;
- CPU training passed;
- offline validation passed;
- unit tests passed;
- readiness strict passed;
- artifacts stayed outside the repo.

The run did not use documented external traces. It used only the synthetic diagnostic smoke traces generated by the Phase 4D pipeline. Therefore it cannot establish external trace behavior, model robustness, or candidate export readiness.

## Observed Model Behavior

The trained smoke model produced only representation `3` on train, validation and OOD diagnostic splits:

```json
{
  "train": {"3": 24},
  "validation": {"3": 12},
  "ood_diagnostic": {"3": 12}
}
```

This is acceptable for a Tier 0 pipeline smoke because the action mask was respected and the training/validation machinery ran. It is not acceptable as an offline candidate selection result.

## Next Selection Gate

The next gate is external trace data:

- documented trace origin;
- normalized `normalized_trace_schema_v1` conversion;
- trace-level train/validation/OOD split;
- train-only normalization;
- leakage audit;
- no fixed-action collapse;
- OOD diagnostic reporting;
- no benchmark or ranking claim.
