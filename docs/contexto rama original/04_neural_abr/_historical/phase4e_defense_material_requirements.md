# Phase 4E — Defense material requirements

Phase 4E must leave material for the thesis and defense.

Required explanations:

- why training is offline;
- why Codex does not decide the method;
- why synthetic smoke is not enough;
- how traces become samples;
- how the teacher labels are generated;
- how future leakage is blocked;
- how train-only normalization is enforced;
- how action masks prevent impossible bitrates;
- why Phase 4E is not benchmark/ranking;
- what must still happen before client integration.

Potential figures:

- trace to dataset pipeline;
- teacher label generation;
- candidate scoring model;
- Phase 4E gates;
- artifact layout outside repository.

Potential tables:

- training commands and seeds;
- dataset split manifest;
- validation gates;
- limitations and mitigations.
